services:
  price_tracker_bot:
    build: .
    container_name: price_tracker_bot
    restart: unless-stopped
    env_file:
      - ../shared/postgres/.env
      - .env
    networks:
      - homelab

networks:
  homelab:
    external: true
